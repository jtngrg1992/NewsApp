export interface Theme {
  primaryColor: string;
  secondaryColor: string;
  backgroundColor: string;
  onPrimary: string;
  onSecondary: string;
  onBackground: string;
  onBackgroundLight: string;
}
